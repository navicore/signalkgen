from setuptools import setup, find_packages

setup(
    name='signal_k_gen',
    version='0.1.0',
    description='generate test json data based on signal k spec',
    author='Ed Sweeney',
    author_email='ed@onextent.com',
    url='https://github.com/navicore/signal_k_gen',
    packages=find_packages(),
)
